//cria a classe node
class Node{ 
    //Cria o construtor da classe que recebe o parâmetro element, que será o elemento enviado
    constructor(element){ re
    //Cria a variável de instancia next, que inicialmente recebe null
    this.next = null;
    //Cria a variável de instancia content, que irá receber o elemento novo do nó
    this.content = element;
    }
}